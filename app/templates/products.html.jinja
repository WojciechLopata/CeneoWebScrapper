{% extends "base.html.jinja" %}

{% block content %}
<ul class="list-group">
    {% for product in products %}
        <li class="list-group-item  justify-content-between align-items-center">
        <div class="row">
          <div class="col-1"><span class = "badge bg-secondary rounded-pill">{{loop.index}}</span> </div>
          <div class="col">  <a class="nav-link" href="{{url_for('product',product_id=product)}}"><i class="bibi-door-open-fill"></i>{{stats[product][0]}}</a> </div>
           <div class="col"> <a>{{stats[product][1]}}</a></div>
            <br>
            <br>
            <div class="col">
            <a href="{{url_for('download',format='csv',product=product)}}">Csv</a>
            <a href="{{url_for('download',format='json',product=product)}}">json</a>
            <a href="{{url_for('download',format='xlsx',product=product)}}">Excel</a>
        <a href="{{url_for('product_delete',product_id=product)}}"> &#128465; Usuń</a>
        </div>
        </div>
        </li>
    {% endfor %}
</ul>

{% endblock content %}